{
    "build": {
        "builder": "nixpacks",
        "buildCommand": "composer install --no-dev --optimize-autoloader && php artisan key:generate"
    },
    "deploy": {
        "startCommand": "php artisan migrate --force && php artisan db:seed --class=UserSeed --force && php -S 0.0.0.0:$PORT public/index.php",
        "restartPolicy": "always"
    }
}