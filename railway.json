{
    "build": {
        "builder": "nixpacks",
        "buildCommand": "composer install --no-dev --optimize-autoloader && php artisan key:generate --force && php artisan storage:link"
    },
    "deploy": {
        "startCommand": "./start-server.sh",
        "restartPolicy": "always"
    }
}